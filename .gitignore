# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
.venv/
env/

# Data sets (these are huge and shouldn't be in version control)
*.csv
*.xlsx

# OS generated files
.DS_Store
Thumbs.db
desktop.ini

# Logs and databases
*.log
*.sqlite
*.db

# API Keys and Secrets
.env
.env.local

# Note: The saved_models/ directory IS included because the API needs them to run,
# but if they get too large in the future, you may need to use Git LFS or ignore them.
